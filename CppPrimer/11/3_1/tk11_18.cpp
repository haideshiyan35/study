#include <iostream>
#include <map>
#include <vector>

using namespace std;

int main()
{
	map<string, size_t> map_cout;

	map<string, size_t>::const_iterator map_it = map_cout.begin();
//	map<string, size_t>::key_value::const_iterator map1_it = map_cout.begin();
}
